The DOCTYPE declaration informs the web browser which version of HTML (or XHTML) the web page is using.
This is important because different versions of HTML may have different rules and standards for rendering web pages.
The browser uses the DOCTYPE declaration to determine how to interpret and display the content.
